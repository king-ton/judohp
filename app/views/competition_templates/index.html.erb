<% provide(:title, t('.title')) %>

<table class="table table-striped tablesorter">
	<thead>
		<tr>
			<th><%= t('activerecord.attributes.competition_template.title') %></th>
			<th><%= t('activerecord.attributes.competition_template.short_name') %></th>
			<th><%= t('venues', count: 1) %></th>
			<th colspan="3"></th>
		</tr>
	</thead>

	<tbody class="competition_template-index">
		<%= render 'index'%>
	</tbody>
</table>

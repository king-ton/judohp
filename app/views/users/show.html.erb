<% provide(:title, @user.name) %>

<p>
	<strong><%= t('activerecord.attributes.user.email') %>:</strong>
	<%= @user.email %>
</p>
<p>
	<strong><%= t('roles', count: @user.roles.count) %>:</strong>
	<% @user.roles.each do |role| %>
	<%= link_to role.name, role_path(role) %><%= ", " if @user.roles.last != role %>
	<% end %>
</p>


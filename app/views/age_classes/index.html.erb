<% provide(:title, t('.title')) %>

<table class="table table-striped tablesorter">
	<thead>
		<tr>
      <th><%= t('activerecord.attributes.age_class.name') %></th>
      <th><%= t('activerecord.attributes.age_class.min_age') %></th>
      <th><%= t('activerecord.attributes.age_class.max_age') %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody  class="age_class-index">
    <%= render 'index' %>
  </tbody>
</table>
